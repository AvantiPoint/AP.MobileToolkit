<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:toolkit="clr-namespace:AP.MobileToolkit.Controls;assembly=AP.MobileToolkit"
             xmlns:converter="clr-namespace:AP.MobileToolkit.Converters;assembly=AP.MobileToolkit"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ToolkitDemo.Views.DoubleRoundingConverterPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Order="Primary" Text="Code Behind" Priority="0" Command="{Binding ShowCodeBehind}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:DoubleRoundingConverter x:Key="DoubleRoundingConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout Padding="20">
        <Label Style="{StaticResource Heading}" Text="Double Rounding Converter" />
        <toolkit:MaterialEntry Text="{Binding Value}" Placeholder="Enter Double Value" />
        <Label Style="{StaticResource Bold}" Text="Converted Value" />
        <Label Text="{Binding Value, Converter={StaticResource DoubleRoundingConverter}}" />
    </StackLayout>
</ContentPage>