<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="ToolkitDemo.Views.APIClientPage">

    <ContentPage.ToolbarItems>
        <ToolbarItem Name="BorderlessDatepickerCodeBehind" Order="Primary" Text="Code Behind" Priority="0" Command="{Binding ShowCodeBehind}"/>
    </ContentPage.ToolbarItems>

    <StackLayout Padding="20">
        <Button Text="Get API Data" Command="{Binding GetApiData}"></Button>

        <Label Text="{Binding APIResult}"></Label>
    </StackLayout>
</ContentPage>