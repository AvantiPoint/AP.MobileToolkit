<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             xmlns:conv="clr-namespace:AP.MobileToolkit.Converters;assembly=AP.MobileToolkit"
             x:Class="ToolkitDemo.Views.UppercaseConverterPage" Title="Uppercase Converter">
    <ContentPage.ToolbarItems>
        <ToolbarItem Name="UppercaseConverterCodeBehind" Order="Primary" Text="Code Behind" Priority="0" Command="{Binding ShowCodeBehind}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <ResourceDictionary>
            <conv:UpperTextConverter x:Key="UpperCaseTextConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout Padding="20">
            <Label Text="Text before conversion:" FontSize="Medium" FontAttributes="Bold"/>
            <Label Text="{Binding TextToBeConverted}"></Label>

            <Label Text="Text after conversion:" FontSize="Medium" FontAttributes="Bold"/>
            <Label Text="{Binding TextToBeConverted, Converter={StaticResource UpperCaseTextConverter}}"></Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>